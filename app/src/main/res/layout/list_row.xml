<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/border"
    android:padding="2dp"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/trackImage"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:scaleType="fitXY" />

    <TextView
        android:id="@+id/trackName"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:text="TextView"
        android:textColor="@android:color/black"
        android:layout_alignTop="@+id/imageButtonFavourite"
        android:layout_toStartOf="@+id/imageButtonFavourite"
        android:layout_marginEnd="25dp"
        android:layout_above="@+id/trackArtist" />

    <TextView
        android:id="@+id/trackArtist"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:text="TextView"
        android:textColor="@android:color/black"
        android:layout_alignBottom="@+id/trackImage"
        android:layout_alignStart="@+id/trackName" />

    <ImageButton
        android:id="@+id/imageButtonFavourite"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="26dp"
        android:layout_marginTop="10dp"
        android:background="@null"
        android:scaleType="fitXY"
        app:srcCompat="@drawable/star_silver" />

</RelativeLayout>